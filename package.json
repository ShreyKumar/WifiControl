{
    "name": "wificontrol-backend",
    "version": "1.0.0",
    "scripts": {
        "client": "cd client && yarn start",
        "server": "nodemon server.js",
        "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
    },
    "dependencies": {
        "body-parser": "^1.19.0",
        "express": "^4.18.2",
        "nodemon": "^1.19.1",
        "stripe": "^7.1.0"
    },
    "devDependencies": {
        "concurrently": "^4.0.1"
    }
}
